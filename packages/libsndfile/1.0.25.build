PACKAGE_URL=http://www.mega-nerd.com/libsndfile/files/libsndfile-1.0.25.tar.gz

CONFIGURE_COMMANDS=\
    ./configure %CONFIGURE_OPTIONS% --disable-external-libs

# TODO: There is a issue with vorbis putting unexpected equals into libsndfile.la . Gotta investigate that
